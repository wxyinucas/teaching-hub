# W2 Draft

> 教师临时收件箱。这里的内容尚未进入本周教学合同；正式依据以 `runbook.md`、`slides.md` 与 `guide.md` 为准。

## 发布学生材料前必须冻结

- 为全班分配不含真实身份的唯一公开课程代号，冻结允许格式；
- 在 GitHub 网页验证 fork、编辑 `signature.toml` 和网页 commit 的实际界面；
- 在干净 WSL 上计时 `clone → uv sync --locked → 程序 → pytest`，记录首次下载与已有缓存两种 `p50/p80`；
- 冻结 Git 与 `uv` 安装黄金路线、最长等待时间，以及保留 Git 信息和依赖缓存的课堂 fallback；
- 准备两人配对表，以及同伴缺席、fork 未改签名或 URL 错误时的 Plan B；
- 用两台不同机器完整实测 Guide，确认输出字段、签名、退出码和 pytest 摘要与材料一致；
- 冻结最后三项记录的收集方式，但不让表单本身成为新的课程负担。

<!--
自由记录尚未归位的想法、链接、案例和课堂观察。

整理时只做三种去向：
- 影响教师判断与取舍 → runbook.md
- 必须让全班共同观看 → slides.md
- 学生需要独立照做或核验 → guide.md

公开仓库中不要写学生隐私、密码、token 或其他敏感信息。
-->
